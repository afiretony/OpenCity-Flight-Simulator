#include "obstacleavoid.h"

glm::vec3 detector::getForwardDir(obstacle* obst, uav* craft)
{
	return glm::vec3();
}

bool detector::isInMap(obstacle obst, std::vector<obstacle> list, glm::vec3 fo)
{
	return false;
}

glm::vec3 detector::getRepulse(obstacle* obst, uav* craft)
{
	return glm::vec3();
}

